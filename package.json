{
  "name": "animal-agent",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "build": "node scripts/sync-frontend.js",
    "check": "node --check server.js && node --check backend/server.js && node --check api/[...path].js && node --check frontend/src/main.js"
  },
  "dependencies": {
    "@vercel/postgres": "^0.10.0"
  },
  "engines": {
    "node": ">=18"
  }
}
